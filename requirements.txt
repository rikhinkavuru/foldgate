# Pure-pip subset for the torch-free reliability layer. For the full comp-bio
# stack (foldseek, US-align, biotite) use pixi.toml or environment.yml instead
# -- those tools are not reliably pip-installable.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8
crepes>=0.9.1
crepes-weighted>=0.1.3
rdkit>=2024.3
posebusters>=0.6
# optional:
# lightgbm>=4.3   # density-ratio weights (watch OpenMP; prefer conda-forge build)
# mapie>=1.4      # RCPS / LTT risk control
