# HANDOFF — foldgate, session of 2026-07-12

**For the next agent/session. The computer was restarted; files on disk persist, session context does not.**
Read this first, then `PROGRESS.md` (the granular durable ledger with every result number and decision).
The auto-memory at `~/.claude/projects/-Users-rikhinkavuru/memory/know-when-to-fold-project.md` also has a
compressed version. The original `HANDOFF.md` is the pre-session plan and is now superseded by this file.

---

## 0. TL;DR

A large SOTA expansion of the `foldgate` reliability layer was completed in one session: **all 4 planned
tiers plus a journal-hardening pass**, each Exa/web-grounded and adversarially vetted. Added 11 new
experiments (E12–E22), 3 new conformal modules, a compliant 4-page MoML paper, and a journal next-steps plan.

- **Tests: 35 pass. Ruff: clean repo-wide. 26 conformal exports.** All new experiments run on CPU from on-disk data.
- **Deliverables:** `paper/moml2026_foldgate.{tex,pdf}` (4 pp main text + refs), `docs/NEXT_STEPS_JOURNAL.md`.
- **Nothing is committed to git** (per working policy, commit only when the user asks). Everything is on disk:
  53 modified + 57 untracked files. Branch `main`, last commit `a1c7fb7`. A new session may want to branch + commit.
- **Blocked items need the user or resources** (see §7): author list, Zenodo DOI, venue choice, prospective
  validation (embargoed data), FoldBench GPU regeneration.

---

## 1. Environment (load-bearing)

- **`uv` + `.venv` (Python 3.12).** Run everything with `.venv/bin/python`. NOT pixi, NOT system python
  (homebrew 3.14, too new). Activation does not persist across separate shell calls; invoke `.venv/bin/python -m ...`.
- Torch-free analysis package (numpy/pandas/scikit-learn/scipy). `tectonic` + `pandoc` available for the PDF;
  `pdfinfo`/`pdftoppm` for page checks. Exa MCP credits are EXHAUSTED — use WebSearch/WebFetch for web research.
- Data on disk: `data/processed/rnp_delivered.parquet` (13,535 × 44, all features joined);
  `data/external/screening/` (Zenodo 17568813 co-folded screens); `data/external/screening_affinity/`
  (Zenodo 18669539 affinity benchmark). RNP raw incl. the 39.5 GB `prediction_files.tar.gz` (already distilled;
  do NOT re-stream).

---

## 2. What was built this session

### Tier 0 — honesty + validity
- **E12 `e12_reliability_drift.py`** — reliability drift D(ν) as a first-class CI'd result. Structural drift
  large + CI excludes 0 all 5 models (Protenix pocket S3 +0.63); temporal ~0. This is *why* the guarantee breaks.
- **E13 `e13_loto_validity.py`** — target-grouped (GroupKFold on system_id) validity; gate holds (AF3 0.185
  [0.168,0.203], 5/5 folds); combined>native AURC survives leakage-free. **Leakage audit: naive pooled row-split
  leaks 95.6% of test targets.**
- Overclaim "provably repairs coverage" removed (PLAN.md, RELATED_WORK.md); CLAUDE.md pixi→uv.

### Tier 1 — FoldBench (feature-limited; JSON tree not public → no GPU regen)
- **E15 `e15_foldbench_transfer.py`** — frozen RNP threshold on FoldBench: AF3 holds ≤α (0.15), Protenix
  under-controls (0.25) = honest mixed transfer; combiner untestable (feature parity). Replaces the E10 negative.

### Tier 2 — the decision number (FLAGSHIP)
- **E16 `e16_selective_screening.py`** — released co-folded screen (Zenodo 17568813): co-folding EF@1% >> docking,
  biggest on novel GPCR (Boltz-2 affinity 26.6 vs Gnina 2.0); ipTM-abstention beats random in 75% of novel targets.

### Tier 3 — depth (all synthetic-validated)
- **E17 `e17_worst_subpop.py` + `src/foldgate/conformal/robust.py`** — label-free worst-subpopulation CVaR
  certificate m*; combined score certifies m*≤0.5 at 25–45% coverage; vacuous on deploy-to-novel (honest boundary).
- **E18 `e18_localized.py` + `conformal/localized.py`** — randomly-localized (Hore-Barber) conformal; synthetic
  marginal coverage 0.907 (vs non-randomized 0.892); beats Mondrian coverage on moderate novelty.
- **E19 `e19_shift_decomp.py` + `conformal/shift_decomp.py`** — signed concept-vs-covariate decomposition;
  AF3-S3 gap 0.328 = 0.309 concept + 0.018 covariate (CI excludes 0) → proves group-conditional is necessary.
- **E14 `e14_disagreement_strata.py`** — training-free deployability surrogate (honest partial).

### Tier 4 — journal hardening (this was the last request)
- **E20 `e20_screening_broad.py`** — 3 models + target-bootstrap CIs + docking head-to-head + a **pre-registered
  RNP-calibrated ipTM gate** (per-model τ, no label peeking) + self-computed novelty shift (GPCR EF 42→27).
- **E21 `e21_affinity_selective.py`** — decoupled 2nd task (ChEMBL Boltz-2 affinity, Zenodo 18669539): novelty
  degradation holds; affinity-head confidence is a weak selective signal (unlike pose ipTM).
- **E22 `e22_robust_certificates.py`** — DRO chi-square robustness radius ρ* (research-confirmed exact 2-point
  Pearson, finite-sample) + **simultaneous certificate across all 5 models** (Bonferroni δ/K: top-20% coverage
  joint m*=0.52, ρ*=0.098, all certified). New `robust.py` functions: `chi2_dro_risk_ucb`, `robustness_radius`,
  `simultaneous_certificate`.
- **Scoop re-swept: cell STILL OPEN.** Baseline-to-beat = Chem Sci D5SC06481C (the E16/E20 data source). Adjacent
  to cite: CONFIDE (2512.02033), GESPI (2509.20345), SCoRE (2603.24704), ConfHit (2603.07371). CLAUDE.md +
  RELATED_WORK scoop notes corrected.
- Repro wiring: `scripts/download_data.py --screening`; Makefile `experiments` runs E1–E22.

Tests for the new theory live in `tests/test_robust.py` (10 tests) and `tests/test_conformal.py` (localized +
decomposition, added by the depth pass).

---

## 3. Deliverables

- **`paper/moml2026_foldgate.pdf`** — 4 pages of main text (log_2022-equivalent style: Times 10pt, US Letter,
  1.5in margins, single column, non-anonymous), references on pages 4–5 (excluded from the limit). Built with
  `cd paper && tectonic moml2026_foldgate.tex`. **Author affiliation is a placeholder — fill before submitting.**
  Fig 1 = E16 screening; Table 1 = AURC/LOTO/drift/m*.
- **`docs/NEXT_STEPS_JOURNAL.md`** — venue strategy (two-paper: Digital Discovery applied + ML/stats methods;
  Nature-tier if a prospective validation lands) and prestige-ranked additions.

---

## 4. Reproduce from scratch

```bash
cd ~/moml
make setup                                   # uv venv + torch-free deps
python scripts/download_data.py              # RNP tabular (52 MB)
python scripts/download_data.py --screening  # co-folded screens + affinity benchmark (E16/E20/E21)
make features                                # -> data/processed/rnp_delivered.parquet
make experiments                             # E1..E22 -> results/*.json + figures
make test                                    # 35 tests
cd paper && tectonic moml2026_foldgate.tex   # -> the 4-page PDF
```

Notes: E16/E20/E21 need the `--screening` download or they no-op gracefully. E22 takes ~2 min (refits the
combiner out-of-fold). The pose-feature pipeline (W1/W2) needs the 39.5 GB structure tarball but is already
distilled into the parquet, so it does not need re-running.

---

## 5. Key result numbers (for the paper / abstract; full detail in PROGRESS.md §5)

| Result | Number |
|---|---|
| E2 break | AF3 marginal risk 0.177 hides S3 0.375 / S4 0.427; deploy-on-novel 0.55, guarantee holds 0/100 runs |
| E12 drift | structural D(ν) +0.4–0.63 (CI excludes 0, all models); temporal ~0 |
| E4 / E13 AURC native→combined | 0.185→0.118 (AF3) … 0.281→0.166 (Protenix); survives leakage-free LOTO |
| E13 leakage | naive pooled split leaks 95.6% of test targets |
| E16/E20 screening | GPCR Boltz-2 affinity EF@1% 26.6 vs docking 1.97; ipTM-abstain beats random in 75% of GPCR targets |
| E17 m* | combined certifies m*≤0.5 at 25–45% coverage; vacuous on deploy-to-novel |
| E19 decomposition | AF3-S3: 0.309 concept + 0.018 covariate (concept CI excludes 0) |
| E22 simultaneous | joint across 5 models at 20% coverage: m*=0.52, ρ*=0.098, all certified |

---

## 6. Git state

Uncommitted. Branch `main`, HEAD `a1c7fb7`. 53 modified + 57 untracked (all this session's work). If you commit,
branch first (do not commit to `main` without asking). Suggested: a feature branch, then a single commit for the
E12–E22 expansion + paper + docs. `.gitignore` excludes `data/`, so the downloaded screens will not be committed
(good). `PROGRESS.md`, `HANDOFF_2026-07-12.md`, `docs/NEXT_STEPS_JOURNAL.md`, and `paper/moml2026_foldgate.*`
should be committed.

---

## 7. What remains (needs the user or resources — all honest blocks)

1. **Author list + affiliation** in the paper; **arXiv/bioRxiv/Zenodo accounts**; **Zenodo DOI** mint from a
   tagged release; **venue choice** (see NEXT_STEPS: Digital Discovery applied paper + a methods paper).
2. **Prospective validation** — the single biggest prestige lever. Mac1 / Cofolding crystal coords + per-compound
   confidences are embargoed; use when released, or partner on a live screen.
3. **Feature-matched FoldBench positive** — needs bounded GPU to regenerate Protenix confidences on the 558
   protein-ligand targets (its pipeline is wired, its JSON emits chain_pair_iptm). Closes the single-benchmark
   weakness with a real positive instead of the current feature-parity-limited transfer.
4. Optional polish: a learned-novelty-coordinate DRO bound; DVC stage for the screening download; a scoop
   re-sweep immediately before submission (watch the Tingjun Hou group and the Ying Jin group).

---

## 8. Load-bearing gotchas

- Env is uv + `.venv` (py3.12), not pixi. Ignore CLAUDE.md's historical pixi text (already corrected).
- Prose style (paper/docs): no em-dashes, no rule-of-three, no negative parallelism. Code/commits: normal.
- The screening gate is a **heuristic** transfer (calibrated on pose correctness, applied to activity) — never
  call it "guaranteed" on the screen. Say so in the paper (it already does).
- The extreme no-analog stratum (S4, n≈76/model) is uncertifiable under every method; the honest action there is
  abstention. Do not claim any method "rescues" it.
- ipTM scale differs across models, so the pre-registered screening gate uses per-model RNP-calibrated thresholds.
- Confidence field names differ per model; verify against a real file before parsing (AF3 emits no PDE).
- Group all pooled analyses by system_id (the 95.6% leakage finding).
